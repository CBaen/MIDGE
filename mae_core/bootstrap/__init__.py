"""Bootstrap package — Mae's 32-layer organism assembly.

Each module handles a group of bootstrap layers. They communicate via
a shared SimpleNamespace context that accumulates all created systems.
"""
